export const environment = {
  production: false,
  api: 'https://localhost:7048',
  // 🍪 Cookie HTTP-Only Configuration
  withCredentials: true // Envia cookies automaticamente em todas requisições
};